services:
  herbivorene:
    container_name: herbivorene
    volumes:
      - ../main.db:/app/main.db
      - ../deploy.txt:/app/deploy.txt
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production